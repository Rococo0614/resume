# 视觉感知项目扩充方案

这份文档用于指导后续补项目，不等同于当前已经完成的简历内容。只有实际实现、验证并留下结果后，才把对应表述写回 `body.tex`。

## 总体主线

三个项目可以统一成一条视觉感知系统链路：

```text
结构化视觉数据 / 场景表征
        ↓
可靠的相机与 ISP 输入
        ↓
高效的端侧感知计算
```

更适合在面试中使用的表述是：

> 关注端侧视觉感知系统，围绕结构化视觉表征、可靠视觉输入和高效端侧计算，完成从图像解析、深度/分割/追踪模型接入，到 VLM 场景理解和 NPU/移动端部署的工程实践。

这里的“更好的数据”不是泛指大规模数据集工程，而是把非结构化视觉输入转化为带有区域、语义、空间关系和时序信息的结构化表征。

## 方向一：结构化视觉表征

### 对应项目

`vivo / LiveAgent｜空间理解与端侧 SDK`

### 当前基础

- 以一张空间拼贴样例为输入解析可复用模板。
- VLM 理解布局、素材关系和组合结构。
- 调用分割、追踪模型提取素材区域和主体信息。
- 用户选择新素材后，根据语义和空间关系完成自动布局。
- 支持滤镜、文本和 DiT 图像变换，并落地 Kotlin 手机端 SDK。

### 建议补强的系统结构

#### 1. 明确模板 Schema

把模板从“模型生成的一段结果”变成稳定的数据结构，至少包含：

```json
{
  "canvas": {"width": 1080, "height": 1920},
  "slots": [
    {
      "slot_id": "foreground_1",
      "role": "person",
      "bbox": [0, 0, 1, 1],
      "z_order": 2,
      "transform": {"scale": 1.0, "rotation": 0.0},
      "constraints": ["inside_canvas", "overlap_background"]
    }
  ],
  "relations": ["foreground_1 in_front_of background_1"],
  "effects": ["filter", "text", "dit_transform"]
}
```

可以根据实际实现删减字段，但要能回答：

- 每个素材区域在哪里。
- 素材是什么语义角色。
- 前后层级是什么。
- 素材之间有什么空间关系。
- 哪些字段由用户素材替换。
- 哪些字段是模板固定约束。
- 滤镜、文本和 DiT 玩法如何挂接。

#### 2. 增加结构化输出校验

建议为 VLM 输出增加：

- JSON Schema 校验。
- 坐标范围校验。
- bbox 与 mask 一致性校验。
- z-order 合法性校验。
- 槽位数量和素材数量校验。
- 关系冲突检查。
- 低置信度结果回退到人工或默认模板。

这部分能把项目从“VLM 生成模板”提升为“视觉表征生成系统”。

#### 3. 做模板解析和布局评测

优先记录以下指标：

| 模块 | 建议指标 |
| --- | --- |
| 模板 JSON | Schema 合法率、字段完整率、解析成功率 |
| 素材区域 | Mask IoU、bbox IoU、边界质量 |
| 主体匹配 | 槽位匹配准确率、素材角色准确率 |
| 空间布局 | 位置误差、尺度误差、层级关系准确率 |
| 追踪关联 | ID 一致性、轨迹断裂次数 |
| 端到端 | 模板复用成功率、布局人工修改率 |

如果暂时没有标注数据，先做小规模人工标注集，并记录失败类型：

- 主体漏分割。
- 多主体混合。
- 前后景关系错误。
- 文本或装饰元素被误识别为素材。
- 素材内容与槽位语义不匹配。
- 遮挡导致追踪或布局失败。

#### 4. 最小可交付成果

建议优先完成：

1. 一份明确的模板 Schema。
2. 一批样例模板和对应结构化标注。
3. 自动化 Schema / 坐标 / 关系校验脚本。
4. 解析失败样例集和错误分类表。
5. 一套模板解析、素材匹配和布局正确率评测脚本。

### 完成后可写入简历的结果句式

```text
设计模板 Schema 统一描述素材槽位、主体语义、层级关系和空间约束，
并通过结构化校验与人工标注集评测模板解析、素材匹配和自动布局效果。
```

有真实数字后再替换为：

```text
模板 Schema 合法率达到 [X]%，素材槽位匹配准确率 [X]%，布局人工修改率降低 [X]%。
```

## 方向二：可靠视觉输入

### 对应项目

`算能 / 可靠视觉输入：图像质量评估与自动化 PQ 调参`

### 当前基础

- 支持 JPEG 对比图和 RAW 图两类输入。
- VLM 负责天黑、逆光、灯光、人像等环境和内容解析。
- RAG 知识库与 JSON 参数配置召回 Top-k 候选。
- 形成样本输入、语义解析、问题归因和参数建议闭环。

### 建议补强的系统结构

#### 1. 统一样本与版本信息

每个样本至少关联：

```text
sample_id
device_id / sensor_id
lens / focal_length
scene_label
RAW or JPEG
ISP version
ISP JSON version
exposure / white-balance metadata
model version
human preference
```

这样才能回答“某个 PQ 问题究竟来自场景、设备、ISP 版本还是模型版本”。

#### 2. 把 VLM 输出固定成结构化标签

建议输出：

```json
{
  "scene": ["night", "portrait", "artificial_light"],
  "content": ["person", "lamp"],
  "issues": ["highlight_clip", "red_cast"],
  "regions": [
    {"name": "skin", "bbox": [100, 120, 300, 520], "issues": ["red_cast"]}
  ],
  "priority": ["skin_tone", "highlight_detail"]
}
```

VLM 负责语义条件和问题分类，像素、颜色和噪声指标由专门的评测模块计算。

#### 3. 建立分层评价指标

不要只保留 PSNR/SSIM，建议分为：

- 像素层：PSNR、SSIM、MAE、LPIPS。
- 颜色层：Lab/OKLab 差异、Delta E、通道偏差、白平衡误差。
- 曝光层：过曝比例、欠曝比例、高光保留、暗部保留、亮度分位点。
- 细节层：SNR、纹理保留、边缘清晰度、halo/ringing。
- 语义层：场景标签准确率、区域问题识别准确率。
- 主观层：A/B 偏好、MOS、人工采纳率。

不同场景使用不同权重：

```text
夜景：暗部噪声 + 高光保留
人像：肤色 + 曝光 + 细节
逆光：高光压制 + 暗部提升
灯光：高光溢出 + 色偏
```

#### 4. 把 JSON 参数修改变成受约束动作

知识库条目建议包括：

```text
scene
symptom
possible_cause
param_path
operation
value_range
step
dependencies
evidence
rollback
```

模型输出不要直接覆盖完整 JSON，而是生成 JSON diff，再经过：

1. Schema 检查。
2. 数值范围检查。
3. 参数依赖检查。
4. 设备/版本兼容检查。
5. 回滚检查。
6. 真实 ISP 回测。

#### 5. 验证对下游感知的影响

这是最能把 AutoPQ 和视觉感知连接起来的补强方向。固定同一批 RAW 样本，比较不同 ISP 参数下：

- 深度估计 Abs Rel、RMSE 或无效点比例。
- 分割 mIoU、边界 F1 或 mask 稳定性。
- 追踪 IDF1、HOTA 或 ID Switch。
- VLM 场景标签一致性。
- 不同曝光、噪声和色偏条件下的鲁棒性。

最终目标不是证明“照片更好看”，而是证明：

```text
ISP 参数优化 → 视觉输入更稳定 → 下游感知结果更可靠
```

#### 6. 最小可交付成果

建议优先完成：

1. 带设备、ISP 版本和 JSON 版本的样本清单。
2. VLM 结构化标签 schema。
3. 颜色、曝光、噪声和细节指标脚本。
4. JSON diff、范围检查和回滚工具。
5. 一组 ISP 参数变化对深度/分割/追踪结果影响的对照实验。

### 完成后可写入简历的结果句式

```text
建立场景—问题—参数—结果关联样本库，结合结构化 VLM 标签、PQ 专项指标和 JSON diff，
完成参数建议的约束校验与真实 ISP 回测，并分析输入质量对下游视觉感知的影响。
```

有真实数字后再替换为：

```text
覆盖 [X] 类场景，参数建议采纳率 [X]%，下游深度/分割/追踪指标提升 [X]%。
```

## 方向三：高效端侧计算

### 对应项目

`算能 / 高效端侧计算：单目深度感知部署与优化`

### 当前基础

- 在 CV186 Mars3/BM1684X 上部署单目深度模型。
- 完成 PyTorch、ONNX 到 TPU 运行时的转换和算子适配。
- 进行计算图裁剪、注意力改写、归一化和 Resample 改造。
- 在 C++ 侧处理异常值和部分算子融合。
- 完成 FP16/INT8 混合量化和实时推理。

### 建议补强的系统结构

#### 1. 建立端侧 Profiling 基线

把模型拆成：

```text
前处理
Encoder
Attention
Decoder
后处理
内存搬运
```

逐模块记录：

- 运行时间。
- 峰值内存。
- 中间张量大小。
- NPU/CPU 占用。
- 带宽或内存搬运量。
- 功耗和温升。
- 算子支持情况。

这样可以说明优化是由哪一个瓶颈驱动，而不是只给出优化前后的总结果。

#### 2. 做量化敏感度和精度回归

按模块记录：

```text
FP32 baseline
FP16 result
INT8 result
混合精度 result
```

并比较：

- 深度 Abs Rel。
- RMSE。
- 边缘区域误差。
- 无效点比例。
- 不同光照场景下的误差。
- 量化误差最大的节点。

最终形成“精度—性能—功耗”的折中表。

#### 3. 从单模型优化扩展到感知流水线

视觉感知系统通常不是只有深度模型，可以尝试串联：

```text
ISP 输出
→ 前处理
→ 分割/检测
→ 深度估计
→ 目标级空间信息
→ 后处理和结构化输出
```

重点观察：

- 多模型是否重复做预处理。
- 中间特征是否可以复用。
- CPU/NPU 之间是否频繁拷贝。
- 模型是否可以异步执行。
- 低置信度时是否触发高精度模型。
- 是否需要轻量模型常驻、重模型按需调用。

#### 4. 补充端侧稳定性指标

建议记录：

- 首帧延迟。
- 稳态 p50/p95 延迟。
- 长时间运行帧率。
- 峰值内存。
- 功耗和温升。
- 掉帧率。
- 模型加载时间。
- 异常输入下的回退行为。
- 量化前后感知精度下降。

#### 5. 把 QNN/MLIR 写成具体实践

如果后续继续补这部分，至少形成一个可讲清楚的案例：

- 模型如何进入 QNN 或 MLIR 流程。
- 哪些算子不支持。
- 如何做 graph rewrite 或 lowering。
- 动态 shape 如何处理。
- 量化校准数据如何选择。
- 如何定位编译后精度异常。
- 是否需要 custom op 或 CPU fallback。

只有形成实际案例后，简历里才建议从“使用过工具链”提升为“完成某模型的 QNN/MLIR 部署与算子适配”。

#### 6. 最小可交付成果

建议优先完成：

1. 一份逐算子 Profiling 表。
2. 一份 FP32/FP16/INT8 精度回归表。
3. 一份显存、带宽、功耗和时延对照表。
4. 一套端侧长时间稳定性测试脚本。
5. 一个包含前处理、模型推理和后处理的端侧感知流水线 Demo。

### 完成后可写入简历的结果句式

```text
建立端侧感知流水线 Profiling 与精度回归流程，定位算子、内存搬运和量化误差瓶颈，
在满足感知精度约束的前提下优化时延、显存、带宽和功耗。
```

当前已经可以使用的量化结果：

```text
参数量 300M → 85M；显存 1.9GB → 0.9GB；带宽 30GB/s → 5GB/s；
17W 功耗约束下实现 1080P@30fps 稳定深度感知。
```

## 推荐补充顺序

### 第一阶段：先补可观测性

优先建立统一日志和样本格式：

```text
输入样本
+ 设备/版本
+ 模型版本
+ 参数 JSON
+ 场景标签
+ 输出结果
+ 指标
+ 失败原因
```

没有可观测性，后面的优化很难证明有效。

### 第二阶段：每个项目各做一个最小闭环

- LiveAgent：模板 Schema + 结构化输出校验 + 小规模人工评测。
- AutoPQ：分层 PQ 指标 + JSON diff 校验 + 一组真实 ISP 回测。
- 深度部署：逐算子 Profiling + 量化前后精度回归。

### 第三阶段：把三个项目连起来

尝试完成一条跨项目实验：

```text
RAW
→ ISP 参数版本 A/B
→ 视觉质量指标
→ 深度/分割/追踪输出
→ 结构化场景结果
→ 端侧性能和鲁棒性比较
```

这会让三个项目从并列经历变成一个完整的视觉感知系统故事。

## 最终求职叙事

完成上述补强后，可以将个人方向概括为：

> 我关注端侧视觉感知系统：通过结构化视觉表征让模型获得可用的场景信息，通过相机与 ISP 质量分析保证输入可靠，再通过模型压缩、算子适配和异构部署让感知系统在端侧实时运行。
